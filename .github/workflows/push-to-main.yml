name: Push to main

on:
  push:
    branches:
      - main
#      - AB2D-6840/fix_ci

permissions:
  contents: read
  id-token: write

jobs:
  test:
    uses: ./.github/workflows/test.yml
    secrets: inherit
  build:
    needs: [test]
    uses: ./.github/workflows/build.yml
    secrets: inherit
